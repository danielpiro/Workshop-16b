package Tests.Bridge;

public class MarketSystem {
    // This will simulate the real application for the market service
    // for now - it will be null (so the proxy implementation will be activated)
}
